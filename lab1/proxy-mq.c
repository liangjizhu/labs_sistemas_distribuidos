/* Hace llamadas a 
 * destroy, set_value,  get_value, delete_key, modify_value y exist 
 * en el lado del cliente
 * Se encarga de realizar las llamadas a servidor
 * Se obtiene biblioteca dinámica -> libclaves.so 
 */